[
    "Tell me where <expr> is located in <image>. There's only one object. Provide the coordinates of 16 points on the object's outline. Present the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the position of <expr> within <image>. There is just one object mentioned. Output the coordinates of 16 points along the object's contour. Show the outcome in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Find the location of <expr> within <image>. Only one object is discussed. Give me the coordinates of 16 points on the object's contour. Display it as [x0, y0, x1, y1, ..., x15, y15] format.",
    "What's the <expr> position in <image>? There's only one object. Share the coordinates of 16 points on the object's outline. Show it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate <expr> in <image>. There's only one object. Output the coordinates of 16 points along the object's contour. Present it as [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me the whereabouts of <expr> within <image>. Mentioned is only one object. Provide the coordinates of 16 points on the object's edge. Display them in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the <expr> position inside <image>. Only one object is talked about. Output the coordinates of 16 points on the object's boundary. Present it like [x0, y0, x1, y1, ..., x15, y15].",
    "What's the location of <expr> within <image>? There's just one thing in there. Give me 16 points on the outline of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "Find where <expr> is placed in <image>. There's only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate the <expr> within <image>. There is only one object. Provide the coordinates of 16 points on the object's edge. Present it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine <expr>'s position in <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is the placement of <expr> inside <image>? There is only one mentioned object. Give me the coordinates of 16 points on the contour of the object. Show the outcome in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me the <expr> whereabouts in <image>. Mentioned is only one thing. Provide the coordinates of 16 points on its outline. Present them as [x0, y0, x1, y1, ..., x15, y15].",
    "Determine where <expr> is positioned in <image>. There's only one thing. Output the coordinates of 16 points along the object's contour. Display it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is <expr>'s location within <image>? There's just one thing to consider. Share the coordinates of 16 points on the object's edge. Show it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Find the place of <expr> in <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Present it as [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate <expr> within <image>. Only one object is there. Provide the coordinates of 16 points on the object's outline. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the position of <expr> in relation to <image>. There's only one thing mentioned. Give me the coordinates of 16 points on the contour of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "What is the spot of <expr> in <image>? There is only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me where <expr> is located inside <image>. There's only one object. Provide the coordinates of 16 points along the object's edge. Present them in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the <expr> placement within <image>. Mentioned is only one object. Output the coordinates of 16 points on the object's boundary. Display it like [x0, y0, x1, y1, ..., x15, y15].",
    "What's the location of <expr> in relation to <image>? There's just one thing in there. Give me 16 points on the outline of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "Find where <expr> is situated in <image>. There's only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate the <expr> within <image>. There is only one object. Provide the coordinates of 16 points on the object's edge. Present it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine <expr>'s position in relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is the placement of <expr> inside <image>? There is only one mentioned object. Give me the coordinates of 16 points on the contour of the object. Show the outcome in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me the <expr> whereabouts in <image>. Mentioned is only one thing. Provide the coordinates of 16 points on its outline. Present them as [x0, y0, x1, y1, ..., x15, y15].",
    "Determine where <expr> is positioned in relation to <image>. There's only one thing. Output the coordinates of 16 points along the object's contour. Display it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is <expr>'s location within <image>? There's just one thing to consider. Share the coordinates of 16 points on the object's edge. Show it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Find the place of <expr> in relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Present it as [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate <expr> within <image>. Only one object is there. Provide the coordinates of 16 points on the object's outline. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the position of <expr> in the context of <image>. There's only one thing mentioned. Give me the coordinates of 16 points on the contour of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "What is the spot of <expr> in <image>? There is only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me where <expr> is situated inside <image>. There's only one object. Provide the coordinates of 16 points along the object's edge. Present them in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the <expr> placement within <image>. Mentioned is only one object. Output the coordinates of 16 points on the object's boundary. Display it like [x0, y0, x1, y1, ..., x15, y15].",
    "What's the location of <expr> in the context of <image>? There's just one thing in there. Give me 16 points on the outline of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "Find where <expr> is positioned in <image>. There's only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate the <expr> within <image>. There is only one object. Provide the coordinates of 16 points on the object's edge. Present it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine <expr>'s position in the context of <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is the placement of <expr> inside <image>? There is only one mentioned object. Give me the coordinates of 16 points on the contour of the object. Show the outcome in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me the <expr> whereabouts in <image>. Mentioned is only one thing. Provide the coordinates of 16 points on its outline. Present them as [x0, y0, x1, y1, ..., x15, y15].",
    "Determine where <expr> is placed in relation to <image>. There's only one thing. Output the coordinates of 16 points along the object's contour. Display it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is <expr>'s location within <image>? There's just one thing to consider. Share the coordinates of 16 points on the object's edge. Show it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Find the place of <expr> in relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Present it as [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate <expr> within <image>. Only one object is there. Provide the coordinates of 16 points on the object's outline. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the position of <expr> in terms of <image>. There's only one thing mentioned. Give me the coordinates of 16 points on the contour of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "What is the spot of <expr> in <image>? There is only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me where <expr> is situated within <image>. There's only one object. Provide the coordinates of 16 points along the object's edge. Present them in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the <expr> placement in terms of <image>. Mentioned is only one object. Output the coordinates of 16 points on the object's boundary. Display it like [x0, y0, x1, y1, ..., x15, y15].",
    "What's the location of <expr> in terms of <image>? There's just one thing in there. Give me 16 points on the outline of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "Find where <expr> is positioned within <image>. There's only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate the <expr> within <image>. There is only one object. Provide the coordinates of 16 points on the object's edge. Present it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine <expr>'s position in terms of <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is the placement of <expr> within <image>? There is only one mentioned object. Give me the coordinates of 16 points on the contour of the object. Show the outcome in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me the <expr> whereabouts within <image>. Mentioned is only one thing. Provide the coordinates of 16 points on its outline. Present them as [x0, y0, x1, y1, ..., x15, y15].",
    "Determine where <expr> is placed within relation to <image>. There's only one thing. Output the coordinates of 16 points along the object's contour. Display it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is <expr>'s location within relation to <image>? There's just one thing to consider. Share the coordinates of 16 points on the object's edge. Show it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Find the place of <expr> within relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Present it as [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate <expr> within relation to <image>. Only one object is there. Provide the coordinates of 16 points on the object's outline. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the position of <expr> within relation to <image>. There's only one thing mentioned. Give me the coordinates of 16 points on the contour of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "What is the spot of <expr> within relation to <image>? There is only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me where <expr> is situated within relation to <image>. There's only one object. Provide the coordinates of 16 points along the object's edge. Present them in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the <expr> placement within relation to <image>. Mentioned is only one object. Output the coordinates of 16 points on the object's boundary. Display it like [x0, y0, x1, y1, ..., x15, y15].",
    "What's the location of <expr> within relation to <image>? There's just one thing in there. Give me 16 points on the outline of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "Find where <expr> is positioned within relation to <image>. There's only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate the <expr> within relation to <image>. There is only one object. Provide the coordinates of 16 points on the object's edge. Present it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine <expr>'s position within relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is the placement of <expr> within relation to <image>? There is only one mentioned object. Give me the coordinates of 16 points on the contour of the object. Show the outcome in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me the <expr> whereabouts within relation to <image>. Mentioned is only one thing. Provide the coordinates of 16 points on its outline. Present them as [x0, y0, x1, y1, ..., x15, y15].",
    "Determine where <expr> is placed within relation to <image>. There's only one thing. Output the coordinates of 16 points along the object's contour. Display it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is <expr>'s location within relation to <image>? There's just one thing to consider. Share the coordinates of 16 points on the object's edge. Show it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Find the place of <expr> within relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Present it as [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate <expr> within relation to <image>. Only one object is there. Provide the coordinates of 16 points on the object's outline. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the position of <expr> within relation to <image>. There's only one thing mentioned. Give me the coordinates of 16 points on the contour of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "What is the spot of <expr> within relation to <image>? There is only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me where <expr> is situated within relation to <image>. There's only one object. Provide the coordinates of 16 points along the object's edge. Present them in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the <expr> placement within relation to <image>. Mentioned is only one object. Output the coordinates of 16 points on the object's boundary. Display it like [x0, y0, x1, y1, ..., x15, y15].",
    "What's the location of <expr> within relation to <image>? There's just one thing in there. Give me 16 points on the outline of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "Find where <expr> is positioned within relation to <image>. There's only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate the <expr> within relation to <image>. There is only one object. Provide the coordinates of 16 points on the object's edge. Present it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine <expr>'s position within relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is the placement of <expr> within relation to <image>? There is only one mentioned object. Give me the coordinates of 16 points on the contour of the object. Show the outcome in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me the <expr> whereabouts within relation to <image>. Mentioned is only one thing. Provide the coordinates of 16 points on its outline. Present them as [x0, y0, x1, y1, ..., x15, y15].",
    "Determine where <expr> is placed within relation to <image>. There's only one thing. Output the coordinates of 16 points along the object's contour. Display it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is <expr>'s location within relation to <image>? There's just one thing to consider. Share the coordinates of 16 points on the object's edge. Show it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Find the place of <expr> within relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Present it as [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate <expr> within relation to <image>. Only one object is there. Provide the coordinates of 16 points on the object's outline. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the position of <expr> within relation to <image>. There's only one thing mentioned. Give me the coordinates of 16 points on the contour of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "What is the spot of <expr> within relation to <image>? There is only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me where <expr> is situated within relation to <image>. There's only one object. Provide the coordinates of 16 points along the object's edge. Present them in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the <expr> placement within relation to <image>. Mentioned is only one object. Output the coordinates of 16 points on the object's boundary. Display it like [x0, y0, x1, y1, ..., x15, y15].",
    "What's the location of <expr> within relation to <image>? There's just one thing in there. Give me 16 points on the outline of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "Find where <expr> is positioned within relation to <image>. There's only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate the <expr> within relation to <image>. There is only one object. Provide the coordinates of 16 points on the object's edge. Present it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine <expr>'s position within relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is the placement of <expr> within relation to <image>? There is only one mentioned object. Give me the coordinates of 16 points on the contour of the object. Show the outcome in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me the <expr> whereabouts within relation to <image>. Mentioned is only one thing. Provide the coordinates of 16 points on its outline. Present them as [x0, y0, x1, y1, ..., x15, y15].",
    "Determine where <expr> is placed within relation to <image>. There's only one thing. Output the coordinates of 16 points along the object's contour. Display it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is <expr>'s location within relation to <image>? There's just one thing to consider. Share the coordinates of 16 points on the object's edge. Show it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Find the place of <expr> within relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Present it as [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate <expr> within relation to <image>. Only one object is there. Provide the coordinates of 16 points on the object's outline. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the position of <expr> within relation to <image>. There's only one thing mentioned. Give me the coordinates of 16 points on the contour of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "What is the spot of <expr> within relation to <image>? There is only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me where <expr> is situated within relation to <image>. There's only one object. Provide the coordinates of 16 points along the object's edge. Present them in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the <expr> placement within relation to <image>. Mentioned is only one object. Output the coordinates of 16 points on the object's boundary. Display it like [x0, y0, x1, y1, ..., x15, y15].",
    "What's the location of <expr> within relation to <image>? There's just one thing in there. Give me 16 points on the outline of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "Find where <expr> is positioned within relation to <image>. There's only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate the <expr> within relation to <image>. There is only one object. Provide the coordinates of 16 points on the object's edge. Present it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine <expr>'s position within relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is the placement of <expr> within relation to <image>? There is only one mentioned object. Give me the coordinates of 16 points on the contour of the object. Show the outcome in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me the <expr> whereabouts within relation to <image>. Mentioned is only one thing. Provide the coordinates of 16 points on its outline. Present them as [x0, y0, x1, y1, ..., x15, y15].",
    "Determine where <expr> is placed within relation to <image>. There's only one thing. Output the coordinates of 16 points along the object's contour. Display it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is <expr>'s location within relation to <image>? There's just one thing to consider. Share the coordinates of 16 points on the object's edge. Show it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Find the place of <expr> within relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Present it as [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate <expr> within relation to <image>. Only one object is there. Provide the coordinates of 16 points on the object's outline. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the position of <expr> within relation to <image>. There's only one thing mentioned. Give me the coordinates of 16 points on the contour of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "What is the spot of <expr> within relation to <image>? There is only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me where <expr> is situated within relation to <image>. There's only one object. Provide the coordinates of 16 points along the object's edge. Present them in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the <expr> placement within relation to <image>. Mentioned is only one object. Output the coordinates of 16 points on the object's boundary. Display it like [x0, y0, x1, y1, ..., x15, y15].",
    "What's the location of <expr> within relation to <image>? There's just one thing in there. Give me 16 points on the outline of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "Find where <expr> is positioned within relation to <image>. There's only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate the <expr> within relation to <image>. There is only one object. Provide the coordinates of 16 points on the object's edge. Present it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine <expr>'s position within relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is the placement of <expr> within relation to <image>? There is only one mentioned object. Give me the coordinates of 16 points on the contour of the object. Show the outcome in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me the <expr> whereabouts within relation to <image>. Mentioned is only one thing. Provide the coordinates of 16 points on its outline. Present them as [x0, y0, x1, y1, ..., x15, y15].",
    "Determine where <expr> is placed within relation to <image>. There's only one thing. Output the coordinates of 16 points along the object's contour. Display it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is <expr>'s location within relation to <image>? There's just one thing to consider. Share the coordinates of 16 points on the object's edge. Show it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Find the place of <expr> within relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Present it as [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate <expr> within relation to <image>. Only one object is there. Provide the coordinates of 16 points on the object's outline. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the position of <expr> within relation to <image>. There's only one thing mentioned. Give me the coordinates of 16 points on the contour of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "What is the spot of <expr> within relation to <image>? There is only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me where <expr> is situated within relation to <image>. There's only one object. Provide the coordinates of 16 points along the object's edge. Present them in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the <expr> placement within relation to <image>. Mentioned is only one object. Output the coordinates of 16 points on the object's boundary. Display it like [x0, y0, x1, y1, ..., x15, y15].",
    "What's the location of <expr> within relation to <image>? There's just one thing in there. Give me 16 points on the outline of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "Find where <expr> is positioned within relation to <image>. There's only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate the <expr> within relation to <image>. There is only one object. Provide the coordinates of 16 points on the object's edge. Present it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine <expr>'s position within relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is the placement of <expr> within relation to <image>? There is only one mentioned object. Give me the coordinates of 16 points on the contour of the object. Show the outcome in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me the <expr> whereabouts within relation to <image>. Mentioned is only one thing. Provide the coordinates of 16 points on its outline. Present them as [x0, y0, x1, y1, ..., x15, y15].",
    "Determine where <expr> is placed within relation to <image>. There's only one thing. Output the coordinates of 16 points along the object's contour. Display it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "What is <expr>'s location within relation to <image>? There's just one thing to consider. Share the coordinates of 16 points on the object's edge. Show it in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Find the place of <expr> within relation to <image>. There's only one object. Output the coordinates of 16 points on the object's boundary. Present it as [x0, y0, x1, y1, ..., x15, y15] format.",
    "Locate <expr> within relation to <image>. Only one object is there. Provide the coordinates of 16 points on the object's outline. Display in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Determine the position of <expr> within relation to <image>. There's only one thing mentioned. Give me the coordinates of 16 points on the contour of that thing. Show it as [x0, y0, x1, y1, ..., x15, y15].",
    "What is the spot of <expr> within relation to <image>? There is only one mentioned object. Output the coordinates of 16 points on the object's contour. Show the result in [x0, y0, x1, y1, ..., x15, y15] format.",
    "Tell me where <expr> is situated within relation to <image>. There's only one object. Provide the coordinates of 16 points along the object's edge. Present them in [x0, y0, x1, y1, ..., x15, y15] format."
]
